# Procedural Vintage Radio

A procedural Three.js reconstruction of a vintage portable radio from a single reference image.

## Local preview

```bash
npm install
npm run dev
```

The preview supports orbit controls, part selection, and an exploded assembly view.

## Production build

```bash
npm run build
```

GitHub Actions deploys the `dist` build to GitHub Pages after pushes to `main`.
