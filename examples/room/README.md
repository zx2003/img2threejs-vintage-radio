# Interactive room reference case

This is the existing hand-authored bedroom/study retained with all user placement and interaction corrections. Its operational entry remains `room.html`; implementation remains under `src/room/` so the existing preview and deployed URL are unchanged.

The door hinge, ground-plane chair dragging, drawer animation, lamp toggle, hover feedback, and reset behavior informed the generic interaction runtime. The generator does not inspect this example or special-case bedrooms.
